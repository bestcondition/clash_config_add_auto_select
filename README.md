# clash_config_add_auto_select
clash config.yaml add auto select proxy


## url

POST or GET
/url=http://example.com/clash_config.yaml

## run

```shell
docker-compose up -d
```

if you change file, run below command
```shell
docker-compose up --build -d
```